@model IEnumerable<Trash_Collector.Models.ApplicationUser>
@using Microsoft.AspNet.Identity

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Wecome @if (User.Identity.Name != null) { @Model.Where(item => item.UserName == User.Identity.GetUserName()).First().firstName }!</h2>

<p>@Html.ActionLink("Choose the day of the week You would like trash pickup","GetDayOfPickup")</p>

<p>This is your information:</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.lastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.addressLine1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.addressLine2)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.city)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.state)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.zipCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.role)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dayOfTheWeek)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.balance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailConfirmed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

    <tr>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().lastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().addressLine1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().addressLine2)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().city)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().state)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().zipCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().role)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().dayOfTheWeek)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().balance)      
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => Model.Where(item => item.UserName == User.Identity.GetUserName()).First().UserName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Id }) |
            @Html.ActionLink("Details", "Details", new { id = Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = Model.Where(item => item.UserName == User.Identity.GetUserName()).First().Id })
        </td>
    </tr>
</table>